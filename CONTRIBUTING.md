# Contributing

Open an issue before a substantial behavior or public-API change. Pull requests must include tests, retain backwards compatibility where practical, pass coding standards, and contain no credentials or real submission data.

Use anonymized fixtures and keep the plugin generally reusable. Site-specific workflows belong in a separate integration plugin using the public enqueue API.
